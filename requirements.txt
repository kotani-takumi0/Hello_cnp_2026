# 再現性のためのバージョン固定。
# ここを揃えないと、同じコード・同じseedでもOOFが一致しない可能性がある。
# 検証環境: Python 3.10.12 (WSL2 / Linux)
#
# セットアップ:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   python3 exp/repro_check.py     # ← 受け入れテスト（必須）
#
# バージョンを上げるときは、必ず repro_check.py を通してから上げること。
# 通らなくなった場合は「環境が変わった」ので、過去のOOF記録と直接比較しない。

numpy==2.2.6
pandas==2.3.2
scipy==1.15.3
scikit-learn==1.7.2
lightgbm==4.6.0
joblib==1.5.3
threadpoolctl==3.6.0

# テキスト特徴（形態素解析）。辞書が変わると企業概要のword TF-IDFが変わるため固定必須
Janome==0.5.0

# 以下は一部のスクリプトでのみ使用
catboost==1.2.10   # make_submission_ensemble.py（現在は未採用: exp012でREJECT）
openai==2.8.1      # colab_openai_embed.py（埋め込み生成。通常はColab側で実行）
